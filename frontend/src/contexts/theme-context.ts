// Re-export theme context from presentation layer
// This maintains the public API while keeping Clean Architecture structure
export { ThemeProvider, useTheme } from '../presentation/contexts/theme-context';

